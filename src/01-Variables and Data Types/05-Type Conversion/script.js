let amount = '100';

// Convert String to Number
// Method 1: amount = parseInt(amount);
// Method 2: amount = +amount;
// Method 3: amount = Number(amount);

// Convert Number to String
// Method 1: amount = amount.toString();
// Method 2: amount = String(amount);

// Convert String to Decimal (i.e. Number)
// amount = parseFloat(amount);

// Convert Number to Boolean - 1 is "true", 0 is "false"
// amount = Boolean(amount);

console.log(amount, typeof amount);